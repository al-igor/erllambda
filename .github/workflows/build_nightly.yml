name: erllambda_dummy

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:

  night_build:
    runs-on: ubuntu-latest
    container:
      image: erlang:22.0.7

    steps:
    - uses: actions/checkout@v2
    - name: Say hello
      run: echo 'Hello world!!'

